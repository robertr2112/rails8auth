# README

This app shows a working Rails 8 with authentication and a basic user model for signup. It also 
uses Bootstrap and simple_form for a simple display format.


* It uses the pg database but could be easily changed to use sqlite
* It does not have a test suite set up

